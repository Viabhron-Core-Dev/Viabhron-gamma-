# 📜 SOP-01: Onboarding Genesis

## 🎯 Purpose
To initialize the Viabhron OS and establish the Sovereign Identity of the Chairman.

## 📋 Procedures
1.  **Identity Registration**: Execute the `identity-8004` module to generate the Chairman's 8004 Digital Passport.
2.  **Treasury Funding**: Connect the `x402` gateway and authorize the initial metabolic budget.
3.  **Sentinel Baseline**: Trigger a full system scan using the `security-radar` to establish a clean state.
4.  **Agent Awakening**: Accreditate the `cloud-manager` and `fiscal-comptroller` roles.

## 💻 Sovereign-Script (SS) Block
```json
{
  "action": "initialize_system",
  "params": {
    "identity_protocol": "8004",
    "treasury_gateway": "x402",
    "security_baseline": "paranoid"
  }
}
```
